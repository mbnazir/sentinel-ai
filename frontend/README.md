# Sentinel AI Dashboard

React + TypeScript investigation dashboard.

## Run

```bash
cd frontend
npm install
npm run dev
```

The dashboard uses the live backend workflow API when available and falls back to demo data when the API is not running.

## Current screens

- investigation queue
- case detail
- evidence list
- comments display
- AI narrative panel
- risk summary cards
